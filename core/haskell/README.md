# Haskell reference · Tham chiếu Haskell

**VI:** `FallDetector.hs` biểu diễn state machine dưới dạng hàm thuần `update`, thuận tiện cho property testing và kiểm chứng quy tắc thời gian. Đây là reference model, không phải lớp camera.  
**EN:** `FallDetector.hs` expresses the state machine as a pure `update` function, useful for property testing and temporal-rule verification. It is a reference model, not a camera adapter.

```bash
ghci core/haskell/FallDetector.hs
```
